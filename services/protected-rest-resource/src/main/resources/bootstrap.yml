server:
  port: 8003

management:
  endpoints:
    web:
      exposure:
        include: '*'

spring:
  application:
    name: protected-rest-resource
  cloud:
    config:
      uri: http://localhost:8888


