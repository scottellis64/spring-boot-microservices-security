spring:
  application:
    name: oauth2-server
  cloud:
    config:
      uri: http://localhost:8888

server:
  port: 8901
  servlet:
    context-path: "/authserver"

management:
  endpoints:
    web:
      exposure:
        include: '*'
